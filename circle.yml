machine:
  java:
    version: openjdk8
deployment:
  release:
    tag: /v*/
    commands:
      - ./gradlew bintraUpload
general:
  artifacts:
    - "build/libs"
    - "build/docs"
    - "build/test-results"